// ILendingPool lp;
        // lp.liquidateCall();
        // uint256 debtToCover = uint(-1); // highest possible liquidation allowed by close factor
        // address debtAsset;
        // bool receiveATokens = false;
        // uint healthFactor = lp.getUserAccountData(user);
        // if (protocol == 1) {}